> “内存优先、数据库后置”的架构优化场景。我们将通过“引入 Redis+布隆过滤器防止重复订单”、“异步持久化最终状态”两个核心步骤来实现改造。

比如订单判重：首先使用布隆过滤器判断是否重复，如果可能重复进入下面流程，
- Redis判断trading:order:processing:ID是否存在，Redis入库后设置DONE状态并延长Ttl为1h便于判重复订单。
- 如果查不到再查OrderBook防止Redis过期。
- 还是不行在查DB。如果还没有则说明

